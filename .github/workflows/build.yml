name: بناء تطبيق درع رمضان

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    name: بناء APK لنظام Android
    runs-on: ubuntu-22.04
    
    steps:
      - name: تحميل الكود من المستودع
        uses: actions/checkout@v4

      - name: إعداد Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: تثبيت Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: تثبيت التبعيات
        run: |
          python -m pip install --upgrade pip
          pip install cython buildozer

      - name: تثبيت حزم النظام المطلوبة
        run: |
          sudo apt update
          sudo apt install -y \
            git \
            zip \
            unzip \
            autoconf \
            libtool \
            pkg-config \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo5 \
            cmake \
            libffi-dev \
            libssl-dev \
            wget

      - name: إعداد Android SDK مع NDK 25
        run: |
          # إنشاء مجلدات Android SDK
          mkdir -p $HOME/.buildozer/android/platform/android-sdk
          cd $HOME/.buildozer/android/platform
          
          # تحميل Android SDK command line tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          
          # إنشاء هيكل المجلدات الجديد
          mkdir -p android-sdk/cmdline-tools
          mv cmdline-tools android-sdk/cmdline-tools/latest
          
          # إنشاء المسار القديم للتوافق
          mkdir -p android-sdk/tools/bin
          
          # إنشاء رابط رمزي للمسار القديم
          ln -sf $HOME/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin/sdkmanager \
                 $HOME/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager
          
          # تعيين متغيرات البيئة
          export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
          
          # قبول التراخيص
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true
          
          # تثبيت المكونات الأساسية - مع NDK 25b (الإصدار المطلوب)
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-31" "build-tools;30.0.3" "ndk;25.1.8937393"
          
          # إنشاء مجلد NDK في المسار الذي يتوقعه Buildozer
          mkdir -p $HOME/.buildozer/android/platform/android-ndk-r25b
          
          # نسخ NDK المثبت إلى المسار المتوقع
          cp -r $ANDROID_HOME/ndk/25.1.8937393/* $HOME/.buildozer/android/platform/android-ndk-r25b/

      - name: بناء التطبيق
        run: |
          # تعيين متغيرات البيئة
          export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
          export ANDROIDNDK=$HOME/.buildozer/android/platform/android-ndk-r25b
          export ANDROIDNDKVER=r25b
          
          # تعديل buildozer.spec لاستخدام NDK 25
          sed -i 's/android.ndk = .*/android.ndk = 25b/g' buildozer.spec
          sed -i 's/android.build_tools = .*/android.build_tools = 30.0.3/g' buildozer.spec
          
          # بناء التطبيق
          buildozer -v android debug

      - name: رفع ملف APK
        uses: actions/upload-artifact@v4
        with:
          name: درع-رمضان-APK
          path: ./bin/*.apk
